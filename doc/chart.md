
# 📈 키움 REST API 차트 데이터 조회 규격서

본 문서는 키움 REST API를 활용하여 국내주식의 차트 데이터(분/일/주/월)를 호출하기 위한 API 명세 요약본입니다.

## 1. 공통 호출 규격

**Method:** `POST` 

**운영 도메인:** `https://api.kiwoom.com` 

**Content-Type:** `application/json;charset=UTF-8` 

**인증 방법:** Header에 `authorization` 필드를 `Bearer {access_token}` 형식으로 포함 


---

## 2. 차트 API 상세 정보

### 2.1 주식 분봉 차트 조회 (ka10080)

주식의 분 단위 차트 데이터를 조회합니다. 

**API ID:** `ka10080` 


* **주요 요청 파라미터 (Body):**
* `stk_cd` (String, 필수): 종목코드 (예: "005930")
* `m_scope` (String, 필수): 분 범위 (예: "1", "3", "5", "10", "30", "60")
* `upd_stkpc_tp` (String, 필수): 수정주가 구분 (0: 미적용, 1: 적용)



### 2.2 주식 일봉 차트 조회 (ka10081)

주식의 일 단위 차트 데이터를 조회합니다. 

**API ID:** `ka10081` 


* **주요 요청 파라미터 (Body):**
* `stk_cd` (String, 필수): 종목코드
* `qry_strt_dt` (String, 필수): 조회 시작일 (YYYYMMDD)
* `upd_stkpc_tp` (String, 필수): 수정주가 구분 (0 또는 1)



### 2.3 주식 주봉 차트 조회 (ka10082)

주식의 주 단위 차트 데이터를 조회합니다. 

**API ID:** `ka10082` 


* **주요 요청 파라미터 (Body):**
* `stk_cd` (String, 필수): 종목코드
* `qry_strt_dt` (String, 필수): 조회 시작일 (YYYYMMDD)
* `qry_end_dt` (String, 필수): 조회 종료일 (YYYYMMDD)
* `upd_stkpc_tp` (String, 필수): 수정주가 구분 (0 또는 1)



### 2.4 주식 월봉 차트 조회 (ka10083)

주식의 월 단위 차트 데이터를 조회합니다. 

**API ID:** `ka10083` 


* **주요 요청 파라미터 (Body):**
* `stk_cd` (String, 필수): 종목코드
* `qry_strt_dt` (String, 필수): 조회 시작월 (YYYYMM)
* `qry_end_dt` (String, 필수): 조회 종료월 (YYYYMM)
* `upd_stkpc_tp` (String, 필수): 수정주가 구분 (0 또는 1)



---

## 3. 공통 응답 구조 및 연속 조회

### 응답 바디 (Common Response Body)

모든 차트 API는 기본적으로 다음과 같은 리스트 형태의 데이터를 반환합니다.

* `output` (LIST): 차트 상세 데이터
* `stk_cd`: 종목코드
* `dt`: 일자/시간
* `open_prc`: 시가
* `high_prc`: 고가
* `low_prc`: 저가
* `close_prc`: 종가
* `trde_qty`: 거래량


`return_code`: 결과 코드 (0: 정상) 

`return_msg`: 결과 메시지 



### 연속 조회 처리 (Paging)

데이터 양이 많아 다음 페이지가 있는 경우 Header를 활용합니다. 

1.**응답 Header**의 `cont-yn` 값이 `Y`이면 추가 데이터가 존재함을 의미합니다. 


2. 다음 데이터 요청 시, **응답 Header**의 `next-key` 값을 **다음 요청 Header**의 `next-key` 필드에 그대로 세팅하여 호출합니다. 



---

## 4. 구현 시 주의사항

* **수정주가:** 권리락 등으로 인한 주가 왜곡을 방지하려면 `upd_stkpc_tp`를 `1`로 설정하는 것이 권장됩니다.
* **에러 핸들링:** `1700` 코드는 호출 제한 초과를 의미하므로, 소규모 트래픽이라도 API 호출 간격(Throttle)을 조절하는 로직이 필요합니다.